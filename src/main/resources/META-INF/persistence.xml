<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="insurancePU" transaction-type="JTA">
        <!--
            MUST match jndiName in server.xml exactly.
            jdbc/InsuranceDB  (no java:app/ prefix — that caused NameNotFoundException before)
        -->
        <jta-data-source>jdbc/InsuranceDB</jta-data-source>

        <properties>
            <!-- Auto-create tables from JPA @Entity classes on first boot -->
            <property name="javax.persistence.schema-generation.database.action" value="create"/>
            <!-- EclipseLink must integrate with Liberty's JTA transaction manager -->
            <property name="eclipselink.target-server" value="WebSphere_Liberty"/>
            <!-- Tell EclipseLink we are talking to Derby -->
            <property name="eclipselink.target-database" value="Derby"/>
        </properties>
    </persistence-unit>

</persistence>